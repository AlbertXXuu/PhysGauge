# Studio viewport evidence

These screenshots and [`viewport-audit.json`](viewport-audit.json) record the bounded PhysGauge
Studio closure check for software `1.1.0-candidate`. The severity-response view is derived from the
immutable v1.0 calibration; the screenshots and browser audit are presentation validation, not new
research evidence.

The audit covers 900, 1024, 1280, 1440, and 1600px widths, the shared Header contract, interaction
targets, keyboard focus, four visual-metric curves across three measured inelastic severity points,
the explicit within-metric normalization, the existing sensitivity matrix, the learned-study
`inconclusive-model` interpretation, and one in-memory two-case smoke calibration.
